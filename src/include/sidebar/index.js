avalon.component('ms-page-sideBar', {
    template: require('./template.html'),
    defaults: {
        path:'',
        onReady(e) {
        },
        onDispose(e) {
            
        }
    }
});